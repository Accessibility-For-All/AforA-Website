User-agent: *
Allow: /

# Confirmation pages carry no standalone value. Extensionless paths, because
# Cloudflare Pages serves /foo (and 308s /foo.html to it) — an .html-suffixed
# Disallow never matches the canonical URL. Prefix match covers both forms.
Disallow: /welcome
Disallow: /thank-you-form-submission
Disallow: /api/

Sitemap: https://accessibilityforall.com/sitemap.xml
