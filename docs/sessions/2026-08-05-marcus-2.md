# Session handoff — 2026-08-05 (session 2, daytime) — Marcus (+ Claude)

**Focus:** Post-launch: merged the two open site PRs, then started the GoHighLevel
marketing-plan build (§05 + Appendix F) via Marcus's logged-in Chrome. Foundations
(fields/tags/pipelines) are DONE; workflows/drips/bot/dashboards are next. A second
five-hat review (plan+site+funnel) was run — see `docs/FIVE-HAT-REVIEW-2026-08-05.md`.

## Shipped / opened
- **PR #28 — merged ✅ (live, verified)** — FAQPage JSON-LD on /pricing (6 items) +
  /docmersion (17 items), pricing duplicate-H1 fix. Verified in production ~90s after
  merge (curl: one H1, schema parses on both pages). Merge was test-merged against
  post-#30 main first — inlined nav/footer preserved.
- **PR #8 — merged ✅ (docs-only)** — Jul-22 GHL work order (GHL-BUILD-CHECKLIST.md,
  ROLLBACK-PRICING.md, EMAIL-TO-JOHN-2026-07-22.md) merged as historical reference.
  Parts are superseded (Formspree/webhook custody); marketing plan §05 + Appendix F
  is the current source of truth.
- **PR #30 was already merged late last night** (not this session — brief said open).

## GoHighLevel build — what EXISTS now (location `it1L0e1mMfZM1WCQIliV`, "Accessibility for All")
All verified in the UI after creation:
- **Custom fields (Contact, "Additional Info" folder)** — all 11 marketing fields now
  exist. Created today (7): Stripe Customer Id, Purchase Date (date), Conformance
  Score (number), Pages Monitored (number), Product Interest, Products Owned, Segment.
  Pre-existing from launch night: Plan, Billing Interval, Scanned Url, Legal Risk Level.
  Keys are clean snake_case ({{contact.stripe_customer_id}} etc.).
- **Tags — all 18 from plan F.5** (22 total incl. 4 legacy): website-lead, wcag-lead,
  signup, demo-booked, customer, plan-starter/growth/scale/enterprise, churn-risk,
  cold-district/gov/assoc, hot-lead, docmersion, websites, reports, response.
- **Pipelines**: **Sales** (New lead 20% → Qualified 40 → Demo booked 60 → Demo held 80
  → Proposal 90 → Closed won 100 → Closed lost 0) and **Self-Serve Customers**
  (New sign-up 20 → Onboarding call 40 → Payment set up 60 → Active 100 → At-risk 50
  → Churned 0). Legacy "Marketing Pipeline" (Apr 7, 10 stages) left untouched — rename
  to zz- next session (Marcus authorized cleanup).
- **Lead scoring — PARTIAL/NOT DONE**: Manage Scoring has 4 template rules (all +1;
  two reference junk tag/calendar). Engagement Score toggle is OFF. Rule deletion
  failed silently twice (see gotchas). Next session: enable toggle, delete the 4
  template rules, add plan rules (email open +2, click +5, tag wcag-lead +30,
  signup +50, demo-booked +40, contact reply +25). MQL 30 / hot 70 thresholds are
  implemented in WORKFLOWS (score-based trigger → owner alert), not in this screen.

## Needs a person
- **John (URGENT, gates all drip email): F.2 sending domain + DKIM/SPF/DMARC.**
  Verified today: accessibilityforall.com has NO SPF/DKIM/DMARC records at all.
  Five-hat C2: no customer-facing workflow email may activate until this passes a
  seed test. Also confirm stephen@accessibilityforall.com actually exists.
- **Marcus**: the free-audit funnel does not exist on the site (five-hat C1 — W2 has
  no producer; websites.html promises an audit but serves the generic contact form).
  Decide: build audit form → `form_type: 'free_audit'` → W2 as a site PR before ads.
- **Marcus**: delete Cloudflare Worker `afora-website`; GHL premium-trigger price
  still unconfirmed; GA4 Key events + Ads import (task queued, do before spend).
- **Stephen**: title/meta rewrites still awaiting his review (unchanged).

## Open items / next work (priority order)
1. **Finish lead scoring** (above) + zz-cleanup: rename legacy Marketing Pipeline,
   delete legacy tags (follow-up, high priority, new-prospect-received, warm lead),
   delete example.org test contacts (Deploy Probe, SignupE2E/AuditE2E Verify,
   Webhook Test-A4A) — they hard-bounce. Marcus authorized deletion 2026-08-05.
2. **W1–W3 actions** — add tags (website-lead / wcag-lead / signup+plan-`<tier>`),
   **internal notifications to Mark + Stephen + Marcus (all three, per Marcus
   2026-08-05 — they're GHL users)**, confirmation emails (BUILD but hold send-
   activation until DKIM passes — five-hat C2). W3 must branch on
   form_type=enterprise-quote → Sales pipeline, not Self-Serve (five-hat M1).
3. **Drips D1–D8 + NEW D9 "Suite Introduction"** (Marcus 2026-08-05: 4 emails over
   4 weeks on signup tag, one per suite tool: Websites, Documents/DocMersion,
   Reports, Response — add to plan docs as D9). Fix merge fields while loading
   (five-hat H2): {domain}→{{contact.scanned_url}}; {issue_count}/{top_issue}/
   {months_left}/{price}/{loss_reason_note} have NO producing field — rewrite or
   create fields. D6.2's "we re-scanned" claim must be made conditional/rewritten.
   Wire W5–W12 per §05. Test everything against marcus@blendwebmarketing.com.
4. **Demo calendar in GHL** (W6 + D3/D4) — five-hat H1: demo funnel currently
   invisible (Calendly books create no contact/tag/GA4 event). Treat as traffic gate.
5. **Conversation AI bot** (Appendix E) + **dashboards** (Appendix G).
6. Site PRs queued: audit-form (C1), privacy-policy refresh (five-hat H4 — page still
   names Formspree/Calendly, missing GoHighLevel/Turnstile), first-touch UTM
   persistence (M8), thank-you noindex (L1), GHL tracking+chat snippet PR coupled
   with privacy update (L2).

## Gotchas found (GHL UI automation — also in memory/)
- Every "create" side-panel/dialog CLOSES after save; the next click lands on the
  underlying page (hit the Business tab twice this way; check tab context).
- "New tag" dialog: the click immediately after a create is often swallowed —
  reopen and VERIFY the dialog exists (find input) before typing; refs go stale
  after each toast/re-render; `form_input` by fresh ref is the only reliable path.
- Custom-field type/folder comboboxes: click option only after it's fully rendered
  (mid-animation clicks silently revert to default). Verify state via screenshot
  before clicking Create.
- Create-pipeline modal auto-REOPENS as a phantom blank "Edit Pipeline" modal after
  each save — Cancel it; do NOT interact (risk of editing Marketing Pipeline).
- Manage Scoring: rule delete confirms but does NOT delete (tried twice, verified
  after reload). May require Engagement Score enabled first, or it's just broken —
  investigate next session.
- The billing banner in agency view is Marcus's business — never click Go to Billing
  (a past session got logged out on the billing page).

## State of main at handoff
- `main` @ b3cd268 (PR #8 merge; PR #28 deployed+verified live before it).
- Production verified: FAQPage schema serving on /pricing + /docmersion.
