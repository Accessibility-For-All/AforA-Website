# Session handoff — 2026-09-24 — Marcus (+ Claude, Cowork cloud session)

**Focus:** The seven-job brief (attribution, lead payload, chat widget, visit pricing, population
field, conversions and engagement, book-demo tracking), plus a CLAUDE.md header fix. **Eight PRs are
open and nothing is merged**, because every merge is a release and Marcus reviews first.

## Shipped / opened (all ⏳ open, none merged)
| PR | Branch | What | Preview |
|---|---|---|---|
| #41 | `chore/claude-md-header` | CLAUDE.md describes Cloudflare Pages + preview limits | docs only |
| #42 | `fix/first-touch-gclid` | **Job 1.** Shared `first-touch.js`; captures click IDs + full UTMs; gclid-only visits stored | fix-first-touch-gclid.afora-website.pages.dev ✅ checked |
| #43 | `feat/lead-payload-clickid` | **Job 2.** Every payload sends the attribution keys; `docs/OFFLINE-CONVERSION-EXPORT.md`. **Stacked on #42** | feat-lead-payload-clickid.afora-website.pages.dev (browser side ✅, HighLevel ❌, see below) |
| #44 | `feat/ghl-chat-widget` | **Job 3.** Inert chat install path (`MODE 'off'`) + keyboard audit | feat-ghl-chat-widget.afora-website.pages.dev ✅ inert |
| #45 | `content/pricing-visit-tiers` | **Job 4.** Visit tiers, onboarding on all, overage policy; report-ladder conflict written up | content-pricing-visit-tiers.afora-website.pages.dev ✅ |
| #46 | `feat/population-served-field` | **Job 5.** Optional `population_served` select on contact, LPs, wizard | feat-population-served-field.afora-website.pages.dev ✅ |
| #47 | `feat/conversion-and-engagement-tracking` | **Job 6.** `conversions.js`; conversions only on 2xx; GTM engagement spec; Clarity advice | feat-conversion-and-engageme.afora-website.pages.dev ✅ failure path |
| #48 | `feat/book-demo-tracking` | **Job 7.** Calendly → GTM `book_demo` spec; demo-length finding | docs only |

**Merge:** #42 before #43. Everything else merges in any order. All 8 were test-merged together
forward and in reverse with no conflicts, and every job's local test suite passes on the merged
tree. After merging JS changes, **purge the zone cache**
(`first-touch.js`, `navbarloader.js`, `contactformloader.js`, `conversions.js`, `chat-widget.js`),
because it holds JS for ~4h.

## Needs a person
**Marcus:**
1. **Job 2 proof. No lead has landed in HighLevel yet.** Preview `/api/lead` answers 500 because
   the Preview env has no GHL webhooks. Pick one: (A) one TEST submission on production after #42
   and #43 merge, or (B) add the three `GHL_WEBHOOK_*` vars to Preview, redeploy, and remove them
   afterwards. The exact JSON bodies from the preview are in the PR #43 comment.
2. **HighLevel mappings** (W1/W2/W3), to the keys actually sent: `first_landing_page`,
   `first_referrer`, `utm_term`, `utm_content`, `gclid`, `gbraid`, `wbraid`, `gclid_captured_at`,
   `population_served` (optional: `msclkid`, `fbclid`). Check whether an empty string overwrites a
   stored gclid.
3. **Labels:** 5 Google Ads labels plus LinkedIn IDs for `conversions.js` (#47), and 1 Book-demo
   label (#48). Then set the GA4-imported copies to **Secondary** so Ads doesn't double count.
4. **GTM (client container):**
   - paste the GHL tracking fix from the Sep 17 handoff (still broken live, `[LC Tracking ERROR]` ×5 per page)
   - build `docs/GTM-ENGAGEMENT-TAGS.md` and `docs/GTM-CALENDLY-BOOK-DEMO.md`
   - register the GA4 custom dimensions listed there
5. **Allow the Chrome extension on analytics.google.com.** It was refused, so the Path Exploration,
   Landing page and Pages and screens reports for the LPs haven't been run.
6. **Real Calendly booking for #48.** Needs the Chrome window in front, since a hidden window stops
   Calendly rendering. It books a real 30-minute Meet on Mark's calendar, so confirm first.
7. **Chat widget:** supply the snippet (then MODE `'preview'` for a keyboard test of *ours*). Decide
   between a local a11y shim and raising the defects with HighLevel. Add a privacy-policy line before `'on'`.
8. **Clarity:** recommendation is don't install yet (#47 has the reasoning and the privacy-copy
   implications).

**Stephen:**
- Definition of a "visit" (sessions or pageviews, whose analytics, what the customer can see).
- The **report ladder** (#45 table). Does "full report on paid tiers" mean signed and human-reviewed?
- No tier now caps document remediation. Is that intended?
- Demo length: the site says 15 min, the Calendly "Sales Demo" is **30 min**, and Stephen says 15–45.
  The booking form also has 7 required questions.
- The Google tag's **automatic user-provided data collection is on** (hashed form emails and phones go
  to Google), and the privacy policy doesn't say so. Describe it or switch it off.

## Open items / next work (raised, not done)
1. **Claim-rule copy outside the PRs:**
   - "compliant" on index, websites, education, non-profit, associations and book-demo
   - the **LP H1 states a bare date**, "April 26, 2027", on the only paid-traffic pages. A fix patch
     has sat unapplied since Sep 17: `Development/patches/2026-09-17-lp-deadline-population-tiers.patch`
   - websites.html's deadline block says "Large public entities" with no number and no 2028 tier
2. **Free check copy:** it's now "free accessibility check", up to 5 pages, results in **one business
   day**. Pages naming the free audit with no turnaround: websites, index, 404, the 4 industry pages,
   and the pricing FAQ. None says "up to 5 pages". None says "homepage-only" either.
3. `styles.css` still carries a dead Tawk.to override. The pricing radiogroup handles Enter/Space but
   not arrow keys. LP and wizard inputs show focus only as a border colour.
4. After #47 goes live: any Ads/GA4 "conversions" series has a **step change that isn't a performance
   change**, because no direct conversion tracking existed before.

## Gotchas found (folded into memory/)
- Branch previews **can't prove HighLevel landing**: no webhooks, Turnstile 110200, and the alias is
  cut to 28 characters → `memory/preview-env-limits.md`.
- The served `gtag/js` and `gtm.js` configs answer "is Enhanced Measurement on / which key events /
  is the GHL tag fixed" without UI access → `memory/tracking-stack.md`.
- HighLevel's chat loader uses **`data-resources-url`** (plural) → `memory/ghl-chat-widget-a11y.md`
  (in #44).
- Cloud Cowork session: no push, no `gh`, egress blocks the site. Ship via git bundle → Mac →
  Desktop Commander → `memory/local-env-tooling-gaps.md`.
- `STEPHEN-UPDATES.md`: its watermark was still at Aug 5, so the Sep 17 merges (#32–#40) had never been summarised. A **draft** entry now covers them plus the open PRs and Stephen's decisions. Marcus reviews and sends it; Claude never sends.

## State of main at handoff
- `main` @ `6eb4c63` (PR #40). Unchanged this session and still live. The Mac clone is clean.

---

# Part 2 — same day, after Marcus's decisions

Marcus's calls: **submit the TEST lead himself**; **Claude creates the conversions** (Secondary
for now, no Purchase action); **do the GTM fixes and publish after a Preview check**; **no Clarity**
(funnels and paths in GA4 instead) plus a **GA4 at-a-glance dashboard**; **apply Stephen's review of
docs 00–04 everywhere**: live ads, website, chatbot and every draft.

## Done (all verified by observation)
| Area | What | Proof |
|---|---|---|
| Google Ads (acct 190-915-1292) | 5 site-tag conversion actions created, all **Secondary**: Free scan, Contact form, Sign-up, Enterprise quote (site tag) and **"A4A Demo booked - Calendly (GTM)"**. The account's tag is **AW-18397428128**. The `AW-957201829` tag the site loads belongs to a **different** account | Labels read back from each action's tag setup |
| Code | #47 `conversions.js` now carries the real ID and 4 labels, plus `a4aEnsureAds()` so the right account is configured before a conversion. #48's Calendly doc carries the book-demo label | `test_job6.py` 15/15 |
| GTM | **Version 3 published** 2026-09-23 22:20 MT: GHL tag fixed (createElement + `data-tracking-id`), GA4 `cta_click`, plan_select→cta_click, LP `scroll_depth`, `lp_form_visible`, Calendly listener, GA4 `book_demo`, Ads book-demo conversion | Preview on production, then the live `gtm.js` (version 3) and a live page: listener present, LC script has `data-tracking-id`, no LC errors |
| GA4 (property 546647180) | Custom dimensions registered. Dashboard **"A4A — Marketing at a glance"** (id 15834843678), filtered to hostname `accessibilityforall.com` | Saved, then re-opened after a full reload |
| Chatbot KB | 5 FAQs rewritten with Stephen's free-scan wording (the agent is still Off) | Re-read via the API |
| Live Google Ads copy | 4 RSAs "Free WCAG Scan: Up to 5 Pages"; descriptions "Free scan of up to 5 … pages"; sitelinks "15-min walkthrough", "Findings mapped to WCAG 2.1 AA"; callout "Onboarding on Every Plan" | Re-read after a reload (RSAs are edited in place and keep their stats) |
| Website | **PR #50** `content/free-scan-accuracy`: both LPs rebuilt on Stephen's lines, sitewide free-check sentences, bare dates given their populations. The pricing FAQ fix went onto **#45** | Harness: no banned phrases, no bare dates, payload keys unchanged; all 9 branches merge cleanly |
| Stephen's review log | `A4A-Stephen-Review-Changes-2026-09-24` (Claude project + Drive doc 07): 23-row change log, corrected D1/D2/webinar/LinkedIn copy, 6 flags | — |

## Preview check detail (GTM v3, production, 2026-09-23)
- One GA4 `page_view` per page. LinkedIn fires once. GHL loads `external-tracking.js` with the ID and logs no errors.
- `/lp/free-audit-deadline/`: `scroll_depth` 25/50/75/90 once each. `cta_click` sent `cta_type=cta`,
  `link_text="Get My Legal Risk Report"`, `link_url=…#audit-form`.
- `/book-demo`: listener present. Same-origin and foreign-origin `calendly.event_scheduled` messages
  were **ignored**, a `calendly.com` `event_type_viewed` passed through, and `page_height` was dropped.
  **No** `book_demo` and **no** request to AW-18397428128 on load.
- **Not verified:** `lp_form_visible`. GTM's visibility trigger doesn't fire while the Chrome window
  is hidden (`document.visibilityState = hidden`), and it was. Check for it in GA4 once real LP traffic arrives.
- A real Calendly booking was **not** made (it would book Mark's calendar). The first real booking
  proves it: `book_demo` once, and one conversion on "A4A Demo booked - Calendly (GTM)".
- Engagement events use `gtag('event')` with no `send_to`, so they also reach `AW-957201829`
  (remarketing only, harmless) and, once #47 is live, AW-18397428128 as non-conversion events.

## Findings
- **GA4 data includes non-production hostnames**: `accessibility-for-all.github.io`, `localhost`,
  `afora-website.pages.dev` and branch previews. Over 28 days, active users drop from 302 to 132 once
  it's filtered to production. The dashboard filters it. Worth a GA4 data filter or a hostname guard.
- **Old campaign-level sitelinks** from Aug 18 are still attached, one with the typo "WCAG 2.11",
  probably on the paused PMax campaign.
- **Two RSAs carry "Scale Plan: 1,000 Documents"** (Gov-Title II Rule, Gov-ADA Title II Law). Change them
  once #45's visit tiers are live, along with the price assets.
- The test browser has a stored `gclid=TESTgclid123` (first-touch from earlier testing), so its hits carry `gclaw=TESTgclid123`.

## Still open
1. **Merge** #42–#48 and #50 (Marcus reviews). After #47 and #48: check a real lead and a real booking
   fire exactly once, then make the site-tag actions **Primary** and the GA4-imported `generate_lead` and
   `sign_up` **Secondary**.
2. GA4: mark `book_demo` as a key event after it first arrives. There's no UI for it before that.
3. HighLevel drafts: load the corrected D1, D2 and webinar copy (Stephen review §3–§4). The builder
   needs the Chrome window visible.
4. LinkedIn: conversions to create, `linkedinIds` in `conversions.js`, draft ad and form copy, new
   image A (account On hold).
5. Replies on Stephen's comment threads in docs 03 and 04. The review docs belong to
   marcusthackston@gmail.com, and Chrome is signed in as marcus@blendwebmarketing.com.
6. After #45 merges: price assets to visit tiers, and the two "Scale Plan: 1,000 Documents" headlines.
7. Chatbot prompt edits (UI only): "A4A's assistant", and put the free scan / partial ACR first.

# Part 3 — merges, tag cleanup, live ads, Stephen audit (late 24 Sep)

Marcus: "Review and Merge PR 42-48 & 50 … Fix the worth knowing, we don't want any bad tags on the site.
And remove the old campaign level sitelinks from August. Change the ads, and update all copy." Then "Merge 51".

## Done (verified by observation)
| What | Where | How verified |
|---|---|---|
| Merged **#42–#48, #50** after reviewing each diff | main | Production pages re-read after deploy |
| **#51** `fix/tags-production-only`, merged | 22 heads, navbarloader, conversions.js, docs | Production: GTM + G-TX5BQW6XZ6 + AW-18397428128 only, no AW-957201829 request, GHL tracking id present, no console errors. github.io mirror now carries the `A4A_PROD` gate, so it sends nothing (and robots is Disallow-all) |
| `lp_form_visible` fires in production | GTM v3 | Seen with the window in front |
| 4 Aug-18 **campaign-level sitelinks removed** (incl. the "WCAG 2.11" typo) | Google Ads | Asset report: 6 sitelinks left, all Account level |
| Both **"Scale Plan: 1,000 Documents"** headlines → "Priced by Monthly Site Visits" | Ads 825076690001, 825077226656 | Reloaded each editor: old 0, new 1 |
| **Price asset** built: Starter $590/yr up to 5,000 visits/mo · Growth $1,490/yr up to 30,000 · Scale $3,990/yr up to 100,000 → /pricing (Account, Service tiers, USD) | Google Ads | Listed in the asset report after save, "Under review". The Sep 19 one had never saved: the account had **no** price asset |
| All 5 RSAs, 6 sitelinks, 8 callouts re-read against Stephen | Google Ads | Nothing left that contradicts him or the pricing page |
| **PR #52** `content/drop-compliant-claims`: "compliant"/"instantly" off 13 public pages + contact-form subtitle; `contactformloader.js?v=20260925` | Website | `test_sweep.py`: every public page rendered, no banned word in text/title/meta, no page errors; `test_stephen.py` and `test_job5.py` still pass |
| Stephen audit: all six docs re-read with comments; **7 unlogged items** added (rows 24–30) + row 31 (ads/pricing) | Project doc + **Drive doc 07 rebuilt** (`1My9Evq2RLkiYHqrnFUBQyKqkiqc888NRN7vlt8jfhoA`; old copy trashed) | Doc read back before the old one was trashed |

## Still open
1. **Marcus: review and merge #52** (diff sent in chat). Also #49 (this docs PR) and #41 (CLAUDE.md).
2. **HighLevel drafts** (D1, D2, WEB-M→WEB-W, WEB-W→WEB-AS, D5/W5, chatbot prompt + agent name): the
   builder renders nothing while the Chrome tab reports `visibilityState = hidden`, and it did all evening
   even with Chrome frontmost per macOS. Needs Marcus's screen showing Chrome.
3. Replies on Stephen's open threads (doc 00 Q2; doc 03; doc 04 ×3) — same Chrome need.
4. LinkedIn copy/form/image A (account On hold; Stephen's OK on rows 6 and 24).
5. After a real lead and a real booking fire once each: Primary/Secondary flip; GA4 `book_demo` key event.
