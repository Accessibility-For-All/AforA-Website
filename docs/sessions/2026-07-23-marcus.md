# Session handoff — 2026-07-23 — Marcus (+ Claude)

**Focus:** Start the post-pricing-wave session: merge the PR wave in order. Marcus ended the
session early after the third merge — this handoff records the exact mid-wave stop point.

## Shipped / opened
- PR #15 — Jul-22 wrap-up trail + `docs/GHL-FIVE-HAT-REVIEW.md` — **merged ✅** (docs only)
- PR #10 — GA4 on all live pages — **merged ✅** (on staging)
- PR #9 — navbar CTA → "Book a demo", #0b6ad4 — **merged ✅** (on staging)
- PR #11 — pricing tiers — **rebased onto post-GA4 main** (the predicted pricing.html-head
  conflict was a blank line; GA4 snippet deduped to exactly 1), force-pushed, **preview
  re-ran green** — open ⏳, ready to merge after a preview spot-check: `…/pr-11/`
- PR #12 (welcome + STRIPE-SETUP), #13 (demo video), #14 (cruft deletion), #8 (GHL docs) —
  **untouched, still open**, previews green as of Jul 22
- This handoff + brief rewrite + Stephen update — PR #16 ⏳ (merge first next session)

## Needs a person
- **Marcus (next session, via Claude):** finish the wave — #11 → #12 → #13 → #14 → #8 (see brief).
- **Marcus:** everything from the Jul-22 list still stands — send the John email (finding 5),
  connect the **A4A** Stripe account (NOT Blend's), branch protection + collaborator audit
  (finding 12).
- **Mark/Stephen:** GHL custody, fair-weekend purchase watcher, per-tier provisioning
  definitions, hosting sign-off — all still open; see the "Open decisions for Marcus/Stephen"
  list at the bottom of `docs/GHL-FIVE-HAT-REVIEW.md`.

## Open items / next work
1. Merge PR #16 (this trail) first, then read `docs/NEXT-SESSION-BRIEF.md` — rewritten for
   the resume point.
2. Resume the wave: spot-check `…/pr-11/pricing.html` preview → merge #11 → #12 → #13 → #14
   (then **hand-prune the 4 deleted cruft files from the `gh-pages` ROOT** — keep_files
   gotcha; exact filenames in the brief) → #8. Then confirm the latest pages-root run is
   green and spot-check staging.
3. Scanner honesty fix (five-hat finding 3) — branch/PR, draft for approval.
4. Legal pages rewrite (finding 4) — privacy for the real stack + short ToS — draft for approval.
5. Five-hat review (a) whole site vs staging; then the Stripe gate (finding 1) if the A4A
   account is connected. `selfServe` stays OFF until go/no-go gates pass.

## Gotchas found
- **PR #11's conflict was exactly as predicted** (Jul-22 brief): both #10 and #11 added the
  identical GA4 snippet at pricing.html's head. Rebase resolution = keep one snippet + main's
  blank line. Verify with `grep -c "googletagmanager.com/gtag/js" pricing.html` → 1.
- **pages-root.yml cancels superseded runs** — after back-to-back merges, intermediate runs
  show `cancelled`; only the LATEST run must be green (it publishes the full main tree).
  Recorded in `memory/pages-root-concurrency.md`.
- GitHub takes ~20–30 s to recompute PR mergeability after main moves — `UNKNOWN` just means
  poll again, not broken.
- The prod S3 deploy failed on every merge tonight — same known OIDC failure
  (`memory/deploy-oidc-broken.md`), NOT new breakage. Staging (Pages root) is the deploy
  target that matters until AWS is fixed.

## State of main at handoff
- `main` @ 475de58 (merge of #9) — staging root verified in sync (pages-root run
  30065693289 green; #10's intermediate run cancelled-by-concurrency as expected).
- Local tree clean; `feat/pricing-tiers` rebased head ed71404 pushed; no unpushed work
  anywhere.
