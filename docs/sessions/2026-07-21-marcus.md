# Session handoff — 2026-07-21 — Marcus

**Focus:** Stand up a version-control + session-notes system for this repo (first use of it).

## Shipped / opened
- PR (this branch `setup/session-workflow`) — adds `CLAUDE.md`, four skills
  (`session-start`, `wrap-up`, `self-check`, `ci-reality-check`), the `docs/` note system,
  and the `memory/` dir; excludes internal dirs from both deploy workflows — **open ⏳**,
  awaiting Marcus's review. Preview URL will be commented on the PR by the bot.

## Needs a person
- **Marcus:** review + merge this PR. Merging deploys to production, but only content files
  ship (internal dirs are excluded), so the live site is unchanged by this PR.
- **Separately:** the CTA-review work lives on `claude/cta-review-improvements-5gb1q7` on the
  **desktop Code** checkout — committed (`7d67a30`, 1 ahead of main) but **not yet pushed**.
  Push it (`git push -u origin claude/cta-review-improvements-5gb1q7`) so it's reviewable; it's
  a version or two behind current `main` and will need rebasing forward before merge.

## Open items / next work
1. Merge this process PR.
2. **⚠️ Fix the production deploy (AWS admin).** `deploy.yml` has NEVER succeeded — OIDC role
   assumption fails (`web identity token could not be validated`), so no merge to main has ever
   deployed; the live site is the original manual S3 sync. Fix is AWS-side (OIDC provider +
   re-apply `aws-setup/trust-policy.json` to role `gha-deploy-soprisapps`, then re-run deploy).
   Full detail: `memory/deploy-oidc-broken.md`. Needs someone with AWS creds on acct 155962534528.
3. Push + rebase the CTA branch onto current `main`, open its PR, review the preview, merge.
   (Branch `claude/cta-review-improvements-5gb1q7` is now pushed to the remote.)
4. Consider a `chore/` PR to delete the legacy `*_--old*.html` / `*_copy_*.html` cruft (confirm first).

## Gotchas found
- `deploy.yml` synced the **whole repo root** to a public S3 bucket with only a partial exclude
  list — `docs/`, `.claude/`, `memory/`, `CLAUDE.md` were NOT excluded. This PR fixes that.
  Any future internal folder must be added to the exclude lists in BOTH workflows.
- Git is the only sync between the desktop, Cowork, and VS Code — unpushed commits are invisible
  to everyone else (this is how the CTA work went missing from review).

## State of main at handoff
- `main` @ 613a284 "New AforA Site" — live at www.soprisapps.com. This PR does not change it.
